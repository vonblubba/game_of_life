<p><%= @current_generation.title %></p>
<p><%= @current_generation.size %></p>
<p><%= @current_generation.grid_text.gsub("\n", "<br>").html_safe %></p>

<%= link_to 'next ->', generation_path(@next_generation) %>
